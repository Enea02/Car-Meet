import 'package:flutter/material.dart';

abstract class AppColors {
  // Backgrounds (warm off-white system)
  static const bg = Color(0xFFFAFAF7);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceMuted = Color(0xFFF2F0EA);
  static const divider = Color(0xFFE6E3DA);

  // Text (warm blacks)
  static const ink = Color(0xFF0E1410);
  static const inkMuted = Color(0xFF55584F);
  static const inkSubtle = Color(0xFF8A8A82);

  // Accent — forest green
  static const accent = Color(0xFF1B4332);
  static const accentSoft = Color(0xFFE6EEE9);

  // Status
  static const success = Color(0xFF2D7A4D);
  static const danger = Color(0xFFB23A2F);
  static const warning = Color(0xFFB5651D);
}
