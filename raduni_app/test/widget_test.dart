// Smoke test placeholder.
// L'app reale richiede l'init di Supabase con env vars,
// quindi un widget test integrale ha senso introdurlo
// dopo aver impostato i mock di Supabase.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
